# API-only mode

Run Greenlight without the Admin UI when agents and scripts are your only
clients.

## Compose

Remove (or comment out) the `greenlight-ui` service from `docker-compose.self-host.yml`.

The gateway on port **8100** is unchanged:

- `POST /v1/channels/new`
- `POST /v1/prompts/new`
- `GET /v1/channels`
- Platform webhooks under `/webhooks/...`

## Auth

For production, set:

```env
USE_AUTH=true
API_KEY=replace-with-a-strong-random-api-key
CALLBACK_SIGNING_SECRET=replace-with-a-strong-random-string
WEBHOOK_SECRET=replace-with-a-strong-random-string
PUBLIC_WEBHOOK_URL=https://api.example.com
```

All agent routes then require `X-API-Key`.

## Managing channels without the UI

```bash
curl -X POST https://api.example.com/v1/channels/new \
  -H "X-API-Key: $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ ... }'

curl https://api.example.com/v1/channels \
  -H "X-API-Key: $API_KEY"
```

See [Agent API](/api-reference/agent-api) for the full surface.
