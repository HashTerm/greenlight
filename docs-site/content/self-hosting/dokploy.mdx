# Dokploy

Deploy Greenlight as a Compose app in [Dokploy](https://dokploy.com/).

## Steps

1. Create a **Compose** application pointing at this repository.
2. Paste variables from `.env.production.example`.
3. Set strong secrets:
   - `API_KEY`
   - `ADMIN_INTERNAL_TOKEN`
   - `AUTH_SECRET`
   - `CALLBACK_SIGNING_SECRET`
   - `WEBHOOK_SECRET`
4. Set `PUBLIC_WEBHOOK_URL` to your public HTTPS API host (**no trailing slash**).
5. Route traffic:
   - API + platform webhooks → `greenlight:8100`
   - Admin UI → `greenlight-ui:3000`
6. Deploy, then register channels via API or the Admin UI.

## Webhook URLs

Each channel uses:

```
{PUBLIC_WEBHOOK_URL}/webhooks/{platform}/{channel_id}
```

Configure that URL in Slack Event Subscriptions, Azure Bot messaging endpoint,
Discord Interactions, Meta Callback URL, Google Chat App URL, etc.

## After deploy

- Open the Admin UI `/setup` once to create the super admin
- Register at least one `PROMPT` channel
- Send a test prompt from the UI or `POST /v1/prompts`

See [Platforms](/platforms) for per-platform webhook details.
