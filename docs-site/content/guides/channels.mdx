# Channels

Channels bind a platform, credentials, and target chat to a stable `channel_id`.

## Register

`POST /register-channel`

```json
{
  "channel_id": "ops-slack",
  "platform": "slack",
  "target_chat_id": "C01234567",
  "credentials": {
    "bot_token": "xoxb-...",
    "signing_secret": "..."
  },
  "callback_url": "https://agent.example/on_message",
  "channel_type": "MESSAGE"
}
```

| Field            | Type   | Notes                                                                                 |
| ---------------- | ------ | ------------------------------------------------------------------------------------- |
| `channel_id`     | string | Required, stable slug                                                                 |
| `platform`       | enum   | `telegram` \| `slack` \| `teams` \| `discord` \| `gchat` \| `whatsapp` \| `messenger` |
| `target_chat_id` | string | Platform-specific chat / channel / space / PSID                                       |
| `credentials`    | object | Platform-specific strings                                                             |
| `callback_url`   | string | Required for `MESSAGE`                                                                |
| `channel_type`   | enum   | `MESSAGE` (default) or `PROMPT`                                                       |

Re-registering an active channel updates config and returns
`already registered (config updated)`.

## Routing rules

- **PROMPT** — many agents or workflows can `POST /v1/prompts` to the same
  `channel_id`. Answers go to each prompt’s `callback_url`, not a shared
  channel callback.
- **MESSAGE** — inbound chat goes to the single `callback_url` set on the
  channel. There is no fan-out to multiple webhooks.
- Prefer **one active `channel_id` per platform + target chat + credentials**.
  If two active rows share that binding, inbound resolution is nondeterministic.

See [Features & benefits](/getting-started/features) for diagrams and when to
choose each mode.

## List and delete

```bash
GET    /channels
DELETE /channels/{id}
```

Both require `X-API-Key` when auth is enabled.

## Send a message

```bash
POST /send
```

Use a registered channel to deliver a plain message (not a prompt card). Useful
after a `MESSAGE` channel callback.

## Platform credentials

See the [Platforms](/platforms) section for per-platform credential tables and
portal setup.
